# 引き継ぎ: レビュー指摘の残り修正 → 全キャンペーン再実行（2026-06-11 作成）

## ミッション

1. 下記「残り修正リスト」をすべて実装する（ユーザー承認済みスコープ: **挙動系全部 + M-8 yaw規約手術を含む**）
2. 検証ゲートを通す
3. 全キャンペーンを新しい outdir で再実行する（プロトコルは下記に完全記載）
4. 4世代比較レポートを作成する

ユーザーは「どれだけ時間がかかってもよい」方針。コミットはユーザーの指示があるまで行わない。

## これまでの経緯（要約）

1. **徹底レビュー実施済み** → `code_review_report_20260611.md`（リポジトリ直下）に確定指摘89件。critical 3件。
2. **修正済み（未コミット、working tree にある）**:
   - **C-1**: `src/pedestrian/observer.py` — ダウンサンプリングの経過時間多重加算（0.4sでなく0.1〜0.3s間隔になっていた）。`_last_update_timestamp` 導入 + 剰余→減算。`last_sample_time` プロパティ追加。
   - **M-1/M-2 時刻アンカー**: `src/prediction/trajectory_predictor.py` — `predict`/`predict_cv`/`predict_single_best` に `staleness` 引数。`process_prediction(pred_traj, anchor_pos, staleness)` で時間軸を「現在の歩行者時刻」に再アンカーし、最終観測位置を t=−staleness の補間アンカーに前置（左端クランプも解消）。`integrated_simulator._update_prediction` が staleness を計算して渡す。**この再アンカーにより metrics 3種（標準ADE/FDE・KDE-NLL・planning_ade）の GT 対応付けは無修正で整合する**（dense index 4k−1 ↔ history[i+4k]、dense index k ↔ history[i+1+k]）。
   - **C-3**: `integrated_simulator._apply_emergency_stop` — 制動中の運動学積分（x,y 前進）+ `copy.copy` で履歴エイリアシング解消。`import copy` 追加済み。
   - **M-9 + ガード**: `frenet_planner.py` — `SINGULARITY_EPS=0.05` クラス定数、`_calc_global_paths` で 1−κ_ref·d ≤ ε の点を含む候補を先頭NaN化で全体無効化、`_check_paths` に非有限値棄却 + **幾何的連続性チェック**（隣接点間距離 > max_speed·dt·3 の「テレポート経路」棄却）。
3. **テスト**: 新規3ファイル（`tests/test_observer.py` 8件、`tests/test_prediction_anchor.py` 6件、`tests/test_planner_guards.py` 9件）。**全93テスト合格**（`.venv/bin/python -m pytest tests/ -q`）。
3b. **セッション2末尾のレビューで追加修正済み（コミット済みの内容に含まれる）**:
   - **F1**: 特異点ガードが域外NaN（スプライン終端超過）も全棄却していた致命バグ → `np.isfinite` 条件で域内特異点のみに限定（域外は従来の切り詰め温存）。これを修正しないと全シナリオでゴール約20m手前から計画不能になる。回帰テスト `test_out_of_domain_paths_are_truncated_not_dropped` 追加済み。
   - **F3**: リプラン分岐（_execute_planning_cycle）の `ego_state.state` インプレース書き換えによる履歴エイリアシング → copy で解消。
   - **F4**: `process_prediction` の target_horizon が staleness 依存になり得た問題（max_t<4.8 設定時に dense 長が変動し metrics から origin が無言脱落）→ `pred_len*sgan_dt` 基準の定数化。
   - metrics.py の standard ADE/FDE docstring を再アンカー後の意味論（補間値評価+最終点の tail 外挿依存）に更新。**修正前後の ADE/FDE 絶対値は非比較である点を論文で明記すること。**
   - **F2（コード変更なし・記録）**: S1/cv の 6mm グレーズ衝突は staleness 配線による正当な挙動変化と二分探索で確認済み（緊急停止修正ではない）。決定論GTの「衝突ゼロ天井」は最終キャンペーンで崩れている可能性が高い。
4. **キャンペーン実行履歴**（output/ 配下、すべて gitignore）:
   - 第1世代（オリジナル、全バグ込み）: `comfort_s{1,2,3}`, `benchmark_rand_s{1,2,3}`, `exp_margin_control`, `exp_margin_control_mc5`, `exp_footprint_uncensored`, `exp_sm_clearance`, `exp_proc_planning`
   - 第2世代 `*_obsfix`（C-1のみ修正）: comfort/rand 全6本完了。
   - 第3世代 `*_anchorfix`（C-1+M-1/M-2）: comfort/rand 全6本完了。`exp_margin_control_anchorfix` 480ラン完了（クリーン）。`exp_footprint_uncensored_anchorfix` 369ラン完了だが **S3×multi-circleの27+27ランが数値発散で汚染**（教訓として保持）。`exp_margin_control_mc5_anchorfix` は途中でkill（不完全）。
   - 第4世代 `*_fixed`（+C-3+M-9ガード）: 起動直後にユーザー指示で停止。**98ランのキャッシュが残っているが、この後コードを変更するため再利用禁止**。`output/exp_*_fixed` は削除するか無視し、最終実行は新サフィックス（`_final` 推奨）を使うこと。
5. **挙動系発見事項**:
   - anchorfix の S3×multi_circle で車両位置が x≈1e14 へ発散するランを発見（M-9特異点 + Δθ≈90°での tanΔθ 爆発 + C-3凍結の相互作用）。ガード追加後は発散解消。ただし「低速・姿勢ずれで候補全滅→停止から復帰不能」の**計画デッドロック**は実挙動として残存（S3 multi_circle seed0: 600ステップ中434計画失敗、ゴール未達）。スプライン端の経路切り詰め（下記修正8）が一因の可能性。
   - comfort_s1 の CV（決定論）が t=3.7s に min_dist 1.194m（閾値1.2m）の**6mmグレーズ衝突** — 正確な予測でプランナが閾値ぎりぎりを通るようになった本物の創発挙動（★1の動機を強める）。M-7格子修正で変わる可能性あり。
   - 修正後の手法ランキングは **CV > LSTM > SGAN**（全シナリオ、Welch p≪0.001）。NLL は LSTM 〜0-3 nats / SGAN 2-12 nats（旧「全モデル16-19natsの重度過信」はバグの産物）。詳細: `docs/OBSFIX_BENCHMARK_RECHECK.md`。

## 残り修正リスト（ユーザー承認済みスコープ）

レビュー指摘の詳細（行番号・根拠・提案）は `code_review_report_20260611.md` を必ず参照。行番号は修正前のもの（現在の working tree では数行ずれている可能性あり）。

### A. 挙動系（プランナ/状態機械、「全部入れる」承認済み）

1. **M-7 横方向格子の対称化**: `frenet_planner.py` `_generate_frenet_paths` の `di_values = np.arange(-max_road_width, max_road_width, d_road_w)` → 0中心対称・d=0を必ず含む格子（例 `np.linspace(-w, w, 2*round(w/d_road_w)+1)`）。S1/S3 の恒常 −0.1m バイアスと左右非対称の除去。
2. **M-10 状態機械の復帰閾値是正**: `state_machine.py:38-46` — デフォルト（caution 0.5 / emergency 1.0m、中心間）が結合半径1.2m未満で復帰距離ゲートが実質無効。デフォルトを結合半径より大きい値（例 caution 2.0 / emergency 3.0m）へ引き上げ、`validate_config` に `safe_distance > ego_radius+ped_radius` を追加。multi_circle 時は `footprint.radius` 基準で換算（data_structures.py:335 とのずれ0.25mも参照）。
3. **CAUTION 速度制約の index 0 除外**: `frenet_planner.py` `_check_paths` の速度判定を index 1 以降に（現在速度が締め付け後の max_speed を超えていると全候補棄却→不要なEMERGENCY昇格）。
4. **スプライン端で切り詰められた経路の無効化**: `_calc_global_paths` で NaN 切り詰めが発生した経路は `len(fp.x) == len(fp.t)` を要求して invalid に（未チェック区間が候補に残る問題 + 長さ1経路が「成功」扱いで状態機械に失敗が伝わらない問題の両方を解消）。S3 デッドロックの緩和が期待される。実装時、ゴール近傍で全候補が落ちる副作用に注意（run() はゴール2m以内で終了するので通常は問題ないはず — S3 multi_circle seed0 で要確認）。
5. **エゴ曲率0仮定の改善**: `frenet_planner.py:242-245` `_cartesian_to_frenet_state` — 前ステップ採用経路の c[1] を使う（ego_state への保持が必要。EgoVehicleState にフィールド追加 or プランナ側に前回値キャッシュ）。
6. **計画ホライゾン端点の包含**: `_build_time_cache` の `np.arange(0.0, time, dt)` → 終端 Ti を含む格子（`np.arange(round(Ti/dt)+1)*dt`）、`Ti` レンジも max_t を含む。**注意**: 障害物配列長との整合（予測50ステップ+前置1=51点、`_hits_dynamic` の clip 挙動）を必ず確認。
7. **M-8 yaw規約の統一（大手術、承認済み）**: 現状は cartesian_to_frenet が返す空間微分 d'=dd/ds を時間グリッド quintic の初期「時間微分」として誤用し、逆変換でも混在（fp.yaw がポリライン接線と最大27°乖離、初期横速度が約 s_d 倍小さい）。修正方針（レビュー提案 (a)）: 時間パラメータ化を維持し、(i) `_cartesian_to_frenet_state` で初期条件に ḋ = v·sin(Δθ)、d̈ も時間微分系で渡す、(ii) `frenet_to_cartesian` へは d' = ḋ/ṡ（ṡ≈0 ガード付き）、d'' も変換して渡す。**回帰テスト必須**: 直線参照・yaw=15°・v=5 の plan() で `fp.yaw` と `arctan2(diff(y), diff(x))` の一致（数度以内）を固定。multi-circle footprint 幾何（`directions` 展開、frenet_planner.py:856 付近）が初めて物理的に正しくなる。Δθ≈±90° での tan 爆発も初期条件側から解消される（連続性ガードは防御層として残す）。

### B. ハーネス/設定系（議論の余地なし、すべて実施）

8. **C-2 メソッド/モデルパス整合**: (i) `trajectory_predictor.py:151-158` の実行時 pooling トグル削除、load_model で checkpoint args の pooling_type と method の整合検証（lstm→'none'系、sgan→pool_net）、不一致は即 raise。(ii) `run_simulation.py` の `--method` 上書き時に `resolve_model_path` 相当でモデルパスも切替え + validate_config 再実行。(iii) `run_statistical_benchmark.py:29-40` `resolve_model_path` のフォールバックを FileNotFoundError に。(iv) `integrated_simulator.py:446-466` 予測の恒常的失敗（連続N回、例N=5）で raise。
9. **M-12 衝突早期終了の集計混入**: IntegratedSimulator.run() が終了理由（goal/collision/timeout）を返すようにし、`run_da_poc.py:98` / `run_footprint_benchmark.py:167` / `benchmark_prediction.py` の goal_reached を終了理由ベースに。衝突ランの time_s は集計から除外 or 別カテゴリ。
10. **M-13 失敗ラン脱落**: `run_statistical_benchmark.py` — 失敗数をカウントして警告+非ゼロ終了、LaTeX キャプションの「over 20 runs」を n_runs から動的生成。
11. **M-14 シード**: `benchmark_prediction.py` に手法ごとの set_seed、`run_simulation.py` に `--seed`（metrics_report.txt に記録）。
12. **M-15 計画時間のリトライ計測**: `integrated_simulator.py` `_execute_planning_cycle` — リトライの plan() も t_plan に加算。
13. **M-6 バリデーション**: `validate_config` に `0.0 <= chance_epsilon < 1.0`、`distribution_aware_planning → num_samples >= 2`。
14. **M-5 派生YAML統一**: scenario_02_cv/_lstm（k_j=0.5,k_d=0.5,k_lat=0.5 残存）、scenario_03_cv/_lstm（k_j=0.5,k_s_dot=0.5,k_lat=1.5 残存）からコスト重みブロック削除、全6派生の num_samples を 20 に。
15. **M-4 default_config.yaml**: マージは実装されておらず誰も読まない死に設定（dataclass とドリフト済み: pred_len 12 vs 8 等）。**推奨: 削除して dataclass を唯一の情報源と CLAUDE.md に明記**（マージ実装より安全）。CLAUDE.md の「マージする」記述も修正。
16. **M-11 再計画ドキュメント整合**: 実装は最大1回/ステップ・上限カウンタはデッドコード。while ループ化（状態がEMERGENCYに達するまで最大 _max_replan_attempts 回）を推奨。ドキュメント側修正でも可（CLAUDE.md「3回/ステップ」）。
17. **（任意・推奨）minor**: min_ttc=inf のランで mean が inf になる問題（NaN化+skipna）、make_margin_report の inf−inf=NaN FAIL、dashboard/animator のしきい値線 1.0→1.2m。他の minor は `code_review_report_20260611.md` §7 参照、時間との相談。

### C. テスト追加（M-16/M-17）

18. **同時刻衝突判定の判別テスト**: 障害物が経路点Pの位置を別時刻に通過→衝突なし / 同時刻→衝突あり / index 0 以外でのみ衝突するケース（`tests/test_frenet_planner.py` に追加）。平坦化退行を検知できるように。
19. **M-8 修正の yaw 回帰テスト**（修正7とセット）。
20. 状態機械「成功2回で回復」シーケンステスト（update() 経由）。

## 検証ゲート（キャンペーン起動前に必ず）

1. `.venv/bin/python -m pytest tests/ -q` 全合格（現92件 + 新規追加分）
2. S3×multi_circle 診断（seed 0, sgan, total_time=60）: 位置が有限かつ物理範囲、発散なし。デッドロックの有無と plan失敗率を記録（修正4・7で改善するか見る）:
```python
# 過去の診断スクリプト例は会話ログ参照。要点:
# cfg = load_config("scenarios/scenario_03.yaml"); cfg.ego_footprint="multi_circle"; cfg.total_time=60.0
# x の範囲・isfinite、plan失敗数/600、最終位置・速度を確認
```
3. comfort_s1 CV seed0 単発（衝突有無と min_dist の変化を記録 — M-7修正で 6mm グレーズが変わる可能性）
4. 直線シナリオ1本（scenario_01 sgan seed0）で ADE/FDE/NLL が anchorfix 値（ade≈0.98）から極端に跳ねないこと（跳ねたら原因を理解してから進む）

## キャンペーン再実行プロトコル

**outdir サフィックスは `_final` を推奨**（`_fixed` は汚染済み部分キャッシュがあるため使用禁止。`output/exp_*_fixed` は削除してよい）。**キャッシュはコード変更をキーに含まないため、コード変更後は必ず新 outdir**。

### Stage 1: exp系（9プロセス並列、リポジトリルートから実行）
```bash
for s in 1 2 3; do
  OMP_NUM_THREADS=1 nohup .venv/bin/python examples/run_da_poc.py \
    --scenarios scenarios/scenario_0${s}.yaml \
    --outdir output/exp_margin_control_final > /tmp/final_mc_s${s}.log 2>&1 &
  OMP_NUM_THREADS=1 nohup .venv/bin/python examples/run_da_poc.py \
    --scenarios scenarios/scenario_0${s}.yaml \
    --ego-footprint multi_circle --ego-footprint-n-circles 5 --total-time 60 \
    --outdir output/exp_margin_control_mc5_final > /tmp/final_mc5_s${s}.log 2>&1 &
  OMP_NUM_THREADS=1 nohup .venv/bin/python examples/run_footprint_benchmark.py \
    --scenarios scenarios/scenario_0${s}.yaml --total-time 60 \
    --outdir output/exp_footprint_uncensored_final > /tmp/final_fp_s${s}.log 2>&1 &
done
```
- 1,329ラン、約3〜5時間（マシン: 10コア）。シナリオ別シャードは同一 outdir 共有で安全（per-run 原子的キャッシュ: da_poc は `runs/<scenario>/<label>/seed_NN.json`、footprint は `runs/<scenario>/<condition>/{method}_seed_NN.json`）。
- 完了後の統合集計（全キャッシュヒット、数分）:
```bash
.venv/bin/python examples/run_da_poc.py --outdir output/exp_margin_control_final
.venv/bin/python examples/run_da_poc.py --ego-footprint multi_circle --ego-footprint-n-circles 5 --total-time 60 --outdir output/exp_margin_control_mc5_final
.venv/bin/python examples/run_footprint_benchmark.py --total-time 60 --outdir output/exp_footprint_uncensored_final --report-only
```
- 注: `exp_sm_clearance` は「clearance化リファクタの挙動保存検証」だったため再実行不要（footprint キャンペーンが実質カバー。レポートに明記）。

### Stage 2: comfort/rand 系（6プロセス並列、Stage 1 完了後）
```bash
for s in 1 2 3; do
  OMP_NUM_THREADS=2 nohup .venv/bin/python examples/run_statistical_benchmark.py \
    --scenario scenarios/scenario_0${s}.yaml --output output/comfort_s${s}_final > /tmp/final_comfort_s${s}.log 2>&1 &
  OMP_NUM_THREADS=2 nohup .venv/bin/python examples/run_statistical_benchmark.py \
    --scenario scenarios/scenario_0${s}.yaml --output output/benchmark_rand_s${s}_final --v0-randomization > /tmp/final_rand_s${s}.log 2>&1 &
done
```
- 303ラン、約1.5時間。完了判定は `output/<dir>/summary_stats.csv` の出現。

### Stage 3: proc_planning（必ず単独・アイドル状態で）
```bash
.venv/bin/python examples/measure_proc_planning.py --outdir output/exp_proc_planning_final
```
- 時間計測なので他プロセスとの並走禁止。デフォルト: scenario_02 / sgan / seed 0 / 4条件。
- M-15 修正によりリトライ込みの計測になる（旧 2.06x との比較時はこの差も明記）。

### 監視の落とし穴（実体験）
- バックグラウンド監視スクリプトは **zsh で動く**: `for d in $var` は単語分割されない → ループ対象はリテラル列挙する。
- `pgrep -f` は監視スクリプト自身のコマンドラインに自己マッチする → パターンに `[k]` ブラケットを入れる（例 `run_da_po[c]`）。
- ログの numba Traceback（typeinfer 等）は pysocialforce の JIT 内部デバッグで無害。実害ある失敗は `summary未生成でプロセス消失` や run_single の `failed` ログで判定。
- 完了通知は Bash run_in_background のラッパは即終了する（nohup 子は detach）ので、Monitor ツールで summary ファイル出現/プロセス消失をポーリングする。

## 最終レポート要件

`docs/FINAL_BENCHMARK_REPORT.md`（名前は任意）に:
1. **系譜表**: 第1世代（オリジナル）→ obsfix → anchorfix → final で「どの修正が入っているか」を明記
2. comfort/rand: 手法ランキング（CV>LSTM>SGAN が維持されるか）、Welch 検定、衝突ラン数、NLL
3. ★1: 主判定「inflation は robust を支配できない」の存否（exp_margin_control_final の Welch 結果と REPORT.md）
4. footprint: Time コスト・S3 デッドロック率（goal_reached）・衝突。multi-circle の旧結論「完走性を損なわない」の存否
5. proc_planning: 2.06x 償却の再計測値（M-15込み）
6. 論文修正が必要な箇所のリスト（旧 ★2 の「S3 SGAN ADE優位消失」は前提消滅、NLL過信の書き換え、など — `docs/OBSFIX_BENCHMARK_RECHECK.md` の「留意点」参照）
7. 既知の限界: S3 multi-circle 計画デッドロック（残存する場合）、ego斥力の等方性（地上真値の楽観性、レビューinfo参照）

メモリ（`~/.claude/projects/-Users-mnhrk-Research-integrated-path-planning/memory/`）の `code-review-20260611-findings.md` と `avec26-research-state.md` も完了時に更新すること（論文表の実体を `*_final` に差し替え）。

## その他の注意

- `calculate_aggregate_metrics(history, dt, prediction_dt=..., prediction_steps=...)` — ego_radius/ped_radius は引数に無い（一度間違えた）。
- SimulationResult に `vehicle_state` 属性は無い（ego_state.state にある）。
- 観測テンソルは float32 経由なのでテストの atol は 1e-5 程度に。
- comfort 系の CV は n=1（決定論）。rand 系は CV も20シード。
- 既存 output/comfort_s*、benchmark_nll_s*、statistical_benchmark* は古い遺物/バグ入りアンカー。挙動保存アンカーとしての旧キャッシュ照合（behavior_check）は今回のコード変更で全 DIFFER になるのが正常。
- 全部終わったらユーザーにコミット可否を確認（observer/predictor/simulator/planner の修正 + テスト3+α + ドキュメント一式が未コミット）。
