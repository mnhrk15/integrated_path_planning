# 引き継ぎ S5: レビュー指摘の修正 → 検証ラダー → Gen-5a キャンペーン（2026-06-11 作成）

前セッション（S4→本セッション）は HANDOVER_20260611_S4.md のミッション3件を実装後、ユーザーのアニメ確認指摘（「2 m/s からの −4 slam + 停止後ジグザグ」）を受けて**緩減速の根本対策**まで実装・検証した。最後に**6観点マルチエージェントレビュー**を実施し、確定前に停止（生指摘を §2 に集約済み）。**新ミッションは §2 の指摘修正 → 検証ラダー → アニメ承認 → キャンペーン起動**。

## 0. 現在の状態

### git（dev、未 push。push は指示があるまで禁止。コミットは段階ごとに可）
adff710 起点で6コミット済み（実装5 + 本ドキュメント 53d5f5d）・**157 テスト全合格**:
- `c320524` S4 ミッション1-3: ブレーキ候補ラダー / footprint オーバーレイ描画+animator 寸法 config 化 / `ego_emergency_decel` 全9 YAML 4.0 統一 / 速度依存トリガ τ
- `9a1d1bd` S1 トリガ再調整（recover 4.2 は EMERGENCY 回復不能で s1_sgan タイムアウト→3.5 に戻した）
- `a05e2f6` 安全速度エンベロープ（CAUTION）+ 適応緊急減速
- `b43ebf5` エンベロープ全状態化 + 停止指令 max_stop_distance + ラダー下限 0.5s + S3 standoff 1.0
- `057103c` エンベロープ/緊急減速を**前方 clearance（clearance_ahead）** に切替（全方位だと群衆内停止後に固着し stall — s1 sgan seed4 で実証）+ S1 recover ゲート 2.0/τ0.25

### 実装の全体像（変更の意図）
1. **ブレーキ候補ラダー**（frenet_planner.py）: Ti=0.5〜min_t 刻み0.5s の quartic 停止候補（横は現在 d ホールド）を常時追加、max_t まで静止サンプルでパディング（同時刻衝突判定の健全性維持）。ピーク減速 1.5v/T は状態別 accel チェックが自然にゲート。
2. **安全速度エンベロープ**（state_machine.py、全状態）: target ≤ √(2·a_env·(clearance_ahead − standoff))。「近いほど遅く」を計画段階で強制。受動ホライゾン計画は停止を毎回引き延ばす（先送り）ため、v_env=0 時は **max_stop_distance**（= clearance_ahead − 0.2）を発行し「残距離内で停止する候補のみ選択可」とする（planner の `_apply_stop_distance_filter`、棄却は `stop_distance_error` バケット）。
3. **適応緊急減速**（integrated_simulator.py `_apply_emergency_stop`）: clip(v²/(2·max(clearance_ahead−0.2, 0.05)), ego_max_accel, ego_emergency_decel)。
4. パラメータ: S1 エンベロープ a2.0/s0.3・トリガ 1.0+0.25v・recover 2.0/2.0、S3 a1.2/s1.0・トリガ 1.0+0.8v・recover 3.5/3.5、**S2 はエンベロープ無効**（強い予防減速は対向流内滞留→衝突、実験済み）。

### 検証済み（output/verify_g5a_s5、smoke_g5a_s5_s*）
- seed1 9ラン衝突ゼロ。**S3 lstm/sgan: hard decel ゼロ・a_min −2.0**（ユーザー指摘のプロファイル解消、滑らかな譲り）。S3 cv は譲り確定時 −3.5 が1回（旧挙動は clearance 0.23 を 3m/s で先抜け＝安全化とのトレードオフ）。S1 は開始時の群衆突入1イベントに集約（clearance 0.06 への割込み含む、物理的に不可避）。
- 5シードスモーク **33/33 goal・衝突ゼロ・タイムアウトゼロ**。
- 分析ワンライナーの雛形: 旧 /tmp/analyze_verify.py（消えていたら npz から a=diff(ego_v)/0.1、hard=a<−2.5 のイベント分割、restarts=(v<0.05→v>0.1) 数を再現）。

## 1. 落とし穴（実験で確認済み、再修正時に踏まないこと）
- エンベロープ/緊急減速は**前方 clearance 必須**（全方位は stall）。回復ゲート 3.5 超は S1 密集流で EMERGENCY 回復不能。
- S1 でエンベロープを強くする（a1.2/s0.5 など）と v_mean<1 でタイムアウト。
- 検証は必ず複数シード（stall は seed4 でのみ発現した）。
- **エンベロープにゼロラグ（同ステップ）clearance を与えない**（S5 で実証: S1 SGAN 3/5 シード stall→timeout・S3 cv limit cycle。§2-2 の注記と 05f3e41 参照）。1ステップ遅れ消費が較正済み仕様。

## 2. 新ミッション: レビュー指摘の修正（優先順）

6観点レビュー（プランナ/状態機械/シミュレータ統合/config・YAML/テスト・クレーム/可視化）の生指摘集約。敵対的検証は未完のため、着手時に各指摘をコードで確認してから直すこと。

### キャンペーン前に必須
1. **[CRITICAL] 適応緊急減速の inf フォールバック**（integrated_simulator.py `_apply_emergency_stop`）: 緊急停止の典型は「横から収束する歩行者で全候補棄却」だが、その瞬間 clearance_ahead=inf → required=0 → **床値 ego_max_accel（従来の半分）でしか制動しない**。静的障害物も映らない。→ `clearance_ahead が非有限なのに計画失敗した場合は従来の最大レート（ego_emergency_decel）にフォールバック`。余裕があれば closing speed（相対接近速度）を required に加味。
2. **[MAJOR] 1ステップ古い clearance**: 各ステップ最初の plan() は前ステップ update() 時の clearance を使う（S1 速度で ~0.75m/step の鮮度差）。→ `_execute_planning_cycle` 冒頭で current_metrics を計算し状態機械に観測させてから `_get_planner_config()`（緊急停止側 `_last_clearance` は同ステップ値で正しい、合わせるだけ）。
   **【2026-06-11 S5 で決着済み — 再適用禁止】** この修正は実装後に実験で棄却した（05fcc85 で実装 → 05f3e41 で巻き戻し）。fresh clearance は接近中系統的に小さくエンベロープを実質強化し、**S1 SGAN 3/5 シードが横断流手前で停止固着→timeout**（smoke_g5a_s6_s1、前回 33/33 goal からの退行）、**S3 cv に 0.17 m/s の plan/緊急停止 limit cycle** を誘発。standoff 0.3→0.2 リチューンでも seed3 は救済不能。1ステップ遅れは較正済みの意図的仕様として `_execute_planning_cycle` にコメント文書化済み（安全クリティカル層＝同時刻衝突判定・緊急停止は元々同ステップ値）。詳細は 05f3e41 のコミットメッセージ参照。
3. **[MINOR だが安全網] `ego_emergency_decel < ego_max_accel` の np.clip 反転**: lo>hi で常に hi が返る。validate_config に関係制約（error か warning）+ テスト。
4. **[MAJOR] 主張の訂正（コード変更なし）**: ①「S2 不変」→ 正しくは「エンベロープ系コミットでは npz ビット一致（検証済み）。c320524 の緊急減速統一（3.0→4.0）と適応化は S2 にも適用（意図的）」。②state_machine.py の「cannot oscillate」コメント — ヒステリシス検証は回復速度時点のみの保証で、S1: trigger@6.0=2.5>ゲート2.0 / S3: trigger@5.0=5.0>3.5 の帯で NORMAL↔CAUTION チャタリングが成立し得る（実害はエンベロープが速度を抑えるため小。最低コメント修正、できれば回復判定を `clearance > max(gate, trigger + τ·v)` に速度対応化 — target 速度で validate を厳格化すると現 S1/S3 設定が invalid になるので注意）。

### 余裕があれば（なければ Gen-5b 以降へ明示先送り）
5. multi_circle 時の clearance_ahead 前方判定が車両中心アンカー（距離は円ごと、判定は中心 — 前半身真横の歩行者の扱い不整合。90°/僅か後方から進入も #1 と同根）。
6. エンベロープ警告に quartic ピーク係数 1.5 を反映（実現停止には 1.5×a_env が要る。S1 の a2.0 は NORMAL 上限 3.0 ちょうど）。
7. STOP_SPEED_EPS=0.15 の隙間（target mod d_t_s ≤0.15 で微速転がり候補を停止扱い）/ スプライン終端で切詰められた停止候補がフィルタに弾かれゴール直前計画失敗の可能性（未テスト）/ グリッド停止候補（Ti<max_t）は未パディング（主張スコープ修正 or 全停止候補パディング）/ ラダー rung の dt 整合（dt=0.2 で銀行家丸め崩れ、現行 0.1 は無事）。
8. ドキュメント陳腐化: 「CAUTION のみ」記述3箇所（config docstring / state_machine / S3 YAML）、S1・S2 の max_road_width コメントが未使用 multi_circle 前提、n_s_sample 死にパラメータ、demo_animation.py の寸法未配管、歩行者描画半径 0.3 vs 衝突 0.2。

### レビューで健全と確認済み（再確認不要）
3点セット完備・9 YAML 派生同期・ラダーパディングの同時刻健全性・停止候補必含/曲率非緩和の不変条件・animator アーティストライフサイクル・copy-on-write・実験クレーム再集計（33/33 goal、S3 hard ゼロ、S2 s4↔s5 ビット一致）。

未消化のレビュー素材: ワークフロー `wf_47945ab9-891`（resumeFromRunId で検証フェーズ再開可、~70エージェント分キャッシュ済み）。生指摘全文は同セッションの tool-results に保存（新セッションからは §2 で十分）。

## 3. 修正後の検証ラダー（毎回この順）
1. `.venv/bin/python -m pytest tests/ -q`（157+）
2. 単発9ラン seed1 → `output/verify_g5a_s6/`（コード変更したら新ディレクトリ）: `for s in 1 2 3; for m in cv lstm sgan: .venv/bin/python examples/run_simulation.py --scenario scenarios/scenario_0${s}.yaml --method $m --seed 1 --output output/verify_g5a_s6/s${s}_${m} [--animate --animation-format mp4]`。比較対象は output/verify_g5a_s5（前回）と output/verify_g5a（S4前）。指標: T_end / v_mean / min_d / a_min / hard イベント（a<−2.5 の連続区間）/ restarts_from_0 / 衝突。**#1 の修正は「前方に歩行者がいないのに計画失敗」したケースの制動を 床値→最大レートに強めるため、hard カウントが微増し得る（安全側で正しい方向）。S1 の既存 slam は元々 cap 適用済みで a_min は不変のはず。S3 の hard ゼロが維持されることを必ず確認**
3. 5シードスモーク `examples/run_statistical_benchmark.py --scenario ... --n-runs 5 --output output/smoke_g5a_s6_s${s}`（キャッシュなし・毎回再実行される）。termination 全 goal・衝突ゼロ
4. **アニメーション（特に s3_sgan の速度プロファイル）をユーザーに提示して承認**

## 4. 承認後: Gen-5a キャンペーン
HANDOVER_20260611_S4.md §6 のコマンド群で、outdir サフィックスを **_g5a2**（コード変更済みのため。さらに変えたら新サフィックス）に置換して起動。正規模: margin_control 480 / footprint 369 / comfort・rand 303。exp 系 ~4時間超。proc_planning は全コード確定後に単独1回。完走後 → Gen-5b（S4 §3: GT footprint 整合 + agent_radius 0.3 統一。worktree `../integrated_path_planning_g5b` は要 rebase）→ docs/FINAL_BENCHMARK_REPORT.md に Gen-5 節追記 → メモリ更新。

## 5. 不変条件・約束事
- 衝突判定は同時刻のみ / 停止プロファイル必含 / 曲率は緩和禁止 / S2 のエンベロープは無効維持（状態機械の S2 特例は意図的）
- 設定キー追加は dataclass + validate_config + save_config の3点セット / base YAML 変更は派生6本も同期
- push 禁止・コミットは段階ごと可 / アニメ承認がキャンペーンのゲート
